<form method="post" action="{{ path('biblio_book_delete', {'id': biblio_book.id}) }}" onsubmit="return confirm('Voulez-vous supprimer définitvement ce livre ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ biblio_book.id) }}">
    <button class="btn">Supprimer de la base de données</button>
</form>
