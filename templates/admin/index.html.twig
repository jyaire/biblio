{% extends 'base.html.twig' %}

{% block title %}Administration de la bilbiothèque{% endblock %}

{% block body %}
    <main class="home">
        <div class="row justify-content-center">
            <h3 class="col-12 text-center p-3">Administration</h3>
            <div class="col-8">
                <a href="{{ path('biblio_book_new') }}" title="Ajouter un livre" class="primary-button">
                    Ajouter un livre
                </a>
            </div>
            <div class="col-8">
                <a href="{{ path('biblio_book_index') }}" title="Liste des livres" class="primary-button">
                    Liste des livres
                </a>
            </div>
            <div class="col-8">
                <a href="{{ path('biblio_emprunt_index') }}" title="Livres sortis" class="primary-button">
                    Livres sortis
                </a>
            </div>
            <div class="col-8">
                <a href="{{ path('biblio_user_new') }}" title="Ajouter un élève" class="secondary-button">
                    Ajouter un élève
                </a>
            </div>
            <div class="col-8">
                <a href="{{ path('biblio_user_index') }}" title="Liste des élèves" class="secondary-button">
                    Liste des élèves
                </a>
            </div>
            <div class="col-8">
                <a href="" title="Chèques de caution" class="secondary-button">
                    Chèques de caution
                </a>
            </div>
            <div class="col-8">
                <a href="{{ path('admin_import') }}" title="Importer des élèves" class="secondary-button">
                    Importer des élèves
                </a>
            </div>
            <div class="col-8 text-center">
                <a href="{{ path('app_logout') }}">Déconnexion</a>
            </div>
        </div>
    </main>
{% endblock %}