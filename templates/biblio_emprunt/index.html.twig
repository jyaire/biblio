{% extends 'base.html.twig' %}

{% block title %}BiblioEmprunt index{% endblock %}

{% block body %}
    <h1>BiblioEmprunt index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>IsEmprunt</th>
                <th>DateEmprunt</th>
                <th>DateRetour</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for biblio_emprunt in biblio_emprunts %}
            <tr>
                <td>{{ biblio_emprunt.id }}</td>
                <td>{{ biblio_emprunt.isEmprunt ? 'Yes' : 'No' }}</td>
                <td>{{ biblio_emprunt.dateEmprunt ? biblio_emprunt.dateEmprunt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ biblio_emprunt.dateRetour ? biblio_emprunt.dateRetour|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('biblio_emprunt_show', {'id': biblio_emprunt.id}) }}">show</a>
                    <a href="{{ path('biblio_emprunt_edit', {'id': biblio_emprunt.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('biblio_emprunt_new') }}">Create new</a>
{% endblock %}
