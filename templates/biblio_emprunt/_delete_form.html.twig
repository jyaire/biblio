<form method="post" action="{{ path('biblio_emprunt_delete', {'id': biblio_emprunt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ biblio_emprunt.id) }}">
    <button class="btn">Delete</button>
</form>
